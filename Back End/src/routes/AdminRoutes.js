const express = require('express')
const router = express.Router();
const { getAdmin } = require('../controllers/AdminController')

console.log("pase por Admin routas")
router.get("/admin", )

module.exports = router;